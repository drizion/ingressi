import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    brutalShadow: {
      borderWidth: 2,
      borderRightWidth: 4,
      borderBottomWidth: 4,
      borderRadius: 10,
    },
    profilePicture: {
      borderRadius: 4,
      borderColor: '#8E8484',
      borderWidth: 2
    },
    elevation: {
      elevation: 10,
      shadowColor: '#000000'
    }
  })